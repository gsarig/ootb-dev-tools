{
  "name": "ootb-dev-tools",
  "version": "1.0.0",
  "description": "Tooling and AI agent prompts for the OOTB OpenStreetMap WordPress plugin",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "plan":      "node scripts/compatibility-check.js --dry-run && node scripts/planning-pipeline.js",
    "compat":    "node scripts/compatibility-check.js",
    "execute":   "node scripts/release-pipeline.js",
    "implement": "node scripts/implement.js",
    "test":      "node scripts/test.js"
  }
}
