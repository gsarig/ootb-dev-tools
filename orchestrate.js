// orchestrate.js
// Automates the handoff between Session A (implementer) and Session B (tester).
// Reads handoff.tmp written by Session A, spawns Session B with it as context,
// reports combined output, and cleans up on success.

// To be implemented — see ootb-implementation-plan.md §5.3
